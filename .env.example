# 复制为 .env.local 并填入真实值（.env* 已 gitignore，绝不提交）

# 必填。只在 API route 里读取，绝不进客户端 bundle。
# 没配时 /api/extract 直接返回空 fields —— 向导照常手填，不报错。
OPENAI_API_KEY=

# 选填，成本刹车。设为 false 可一键停掉所有真实模型调用
#（示例案例是静态数据，不受影响）。不设即为开启。
# AI_ENABLED=false
