export function SiteFooter() {
  return (
    <footer className="border-t border-foreground/10 px-4 py-6 text-center text-xs text-muted">
      <p>本工具提供信息辅助，不构成法律意见。</p>
      <p className="mt-1">押金侠 BondBack · 数据仅在本次会话内使用，不做存储</p>
    </footer>
  );
}
