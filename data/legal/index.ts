import type { AUState, LegalClause, StateProcess } from "./types";

// TODO(任务 2)：产出 nsw.ts / vic.ts 后在此汇总导出。
export const legalClauses: Record<AUState, LegalClause[]> = {
  NSW: [],
  VIC: [],
};

export const stateProcesses: Record<AUState, StateProcess[]> = {
  NSW: [],
  VIC: [],
};
