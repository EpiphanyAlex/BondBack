# 06 · 入住留证模式 —— **已取消（2026-07-25）**

> 母文档 §6。原为「Gate A 后条件解锁」，v1.1 起**硬砍为愿景展示**，本模块不写一行代码。

## 决定

- **不实现**。首页愿景橱窗中的「入住留证」条目即为终态。
- **Gate A（主流程端到端跑通并部署）不再带解锁含义**，仅作为主线健康检查。

## 为什么

1. 它对「证据驱动的退租维权闭环」这条差异化叙事几乎不加分——评审看的是第二层的对照能力，不是多一个入口。
2. 条件解锁本身有成本：意味着要在 7/25 深夜、最疲劳的时候做一次「要不要动工」的判断，而 v1.1 已给主线新增了事实提取管线、举证翻转模式、证据档、首页对照卡、`/sample` 重放、可定位入住报告六项工作。
3. 提前砍掉可以把这份决策带宽还给主线。

## 愿景橱窗里怎么写（04 负责）

> 即将支持：QLD / WA · **入住留证**（拍照生成带时间戳的 condition report） · 中介往来助手

保持「即将支持」的措辞，不得暗示当前可用。
